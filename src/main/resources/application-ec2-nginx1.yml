server:
  port: 8081
spring:
  profiles:
    include: ec2-nginx1, ec2-db
  jpa:
    properties:
      hibernate:
        dialect:
          nodeValue:
            org.hibernate.dialect.MySQL5InnoDBDialect
          storage_engine: innodb
  data:
    web:
      pageable:
        default-page-size: 10
        one-indexed-parameters: true